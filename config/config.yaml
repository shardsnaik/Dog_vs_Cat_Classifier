artifacts_folder: artifacts

downloading_data:
  source_dir: artifacts/raw_data
  source_URL: karakaggle/kaggle-cat-vs-dog-dataset

preproccessed_data:
  datasets_dir: artifacts/datasets
  train_dir: artifacts/datasets/train
  test_dir: artifacts/datasets/test

image_categories:
  - train/Cat
  - train/Dog
  - test/Cat
  - test/Dog

trained_model:
  trained_model_path: artifacts/trained_model
  datasets_dir: artifacts/datasets
  train_dir: artifacts/datasets/train
  test_dir: artifacts/datasets/test